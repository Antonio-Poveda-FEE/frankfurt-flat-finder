# Optional overrides. The app ships with these baked into src/lib/config.ts,
# so you normally don't need a .env file at all. Set these only if you want to
# point the app at a different Supabase project locally.
VITE_SUPABASE_URL=
VITE_SUPABASE_KEY=
